import React from "react";
import { Navbar, Nav } from "react-bootstrap";

const HeaderAdmin = (props) => {
    return (
        <Navbar className="r-0" expand="lg">
            <Navbar.Brand>Page Heading</Navbar.Brand>
        </Navbar>
    );
}

export default HeaderAdmin;